name: Benchmark

on:
  workflow_dispatch:        # run manually from Actions tab
  schedule:
    - cron: "0 3 * * 1"    # every Monday at 3am UTC
  push:
    paths:
      - "benchmark/**"     # also run when benchmark code changes

jobs:
  benchmark:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Run benchmark (built-in engine, no deps)
        run: |
          python benchmark/run.py --no-sweep
          echo "── Stateless Results ──"
          cat benchmark/results/summary.json

          python benchmark/run.py --no-sweep --context
          echo "── Context-Aware Results ──"
          cat benchmark/results/context_summary.json

      - name: Upload results as artifact
        uses: actions/upload-artifact@v6
        with:
          name: benchmark-results-${{ github.sha }}
          path: benchmark/results/
          retention-days: 90
